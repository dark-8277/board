# 1. 웹 서버의 루트 폴더 지정
DocumentRoot "/var/www"

# 2. /var/www 폴더에 대한 접근 권한 허용
<Directory "/var/www">
    Options Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>

# 3. 기존에 있던 설정
DirectoryIndex login.html index.html index.php